# macOS Work Planner

## Requires: 

``` bash
conda create -n mac_devs python=3.11
pip install pyobjc-framework-EventKit
```


Usage:

Only works using Terminal app of Macos
```bash
python work_planner.py --project "heat island modelling" --hours 2 --deadline 25-05
```